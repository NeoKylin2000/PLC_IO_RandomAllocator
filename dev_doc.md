# PLC 实训考核 I/O 随机分配器 — 开发文档

## 一、项目概述

本项目用于 PLC 实训模拟考试场景，对 5 个 PLC 实训项目的 I/O 点地址进行随机重新分配，避免考生背诵固定地址，保证考核公平性。

**数据来源**：《新实训题 电子及PLC》文档中 5 个 PLC 实训项目的 I/O 分配表。

**分配规则**：单选模式，每次只选择一个项目进行随机分配，从八进制地址池中随机抽取（项目内地址不重复）。

## 二、功能特性

| 功能 | 说明 |
|------|------|
| 项目单选 | 左侧列表通过单选按钮选择一个实训项目，每次只能选一个 |
| 随机分配 | 对当前选中的项目执行 I/O 地址随机分配 |
| 结果展示 | 右侧表格展示当前项目的分配结果，输入点蓝色、输出点绿色 |
| 复制当前 | 将当前查看项目的分配结果复制到剪贴板 |
| 复制全部 | 将所有已分配项目的结果复制到剪贴板 |
| 导出 TXT | 将全部分配结果导出为文本文件，支持自定义保存路径 |
| 重置 | 清空所有分配结果 |
| 自动分配 | 程序启动时自动对默认选中项目执行一次随机分配，打开即见结果 |

## 三、技术架构

- **编程语言**：Python 3.14
- **GUI 框架**：tkinter + ttk（Python 标准库，无需额外依赖）
- **界面风格**：Windows 11 浅色卡片风格
- **打包工具**：PyInstaller 6.21.0
- **运行平台**：Windows 10/11

## 四、数据结构

### 4.1 项目数据

每个 PLC 实训项目包含以下字段：

```
{
    "id":      项目唯一标识（如 "plc1"）
    "name":    项目全称
    "short":   简称（用于左侧列表显示）
    "inputs":  输入点功能名称列表
    "outputs": 输出点功能名称列表
}
```

### 4.2 五个实训项目 I/O 统计

| 项目 | 输入点数 | 输出点数 |
|------|---------|---------|
| PLC实训一 多种液体混合 | 11 | 4 |
| PLC实训二 花样喷泉 | 4 | 4 |
| PLC实训三 交通灯 | 3 | 6 |
| PLC实训四 简易机械手 | 12 | 8 |
| PLC实训五 运输传送线 | 12 | 5 |

## 五、核心算法

### 5.1 八进制地址池

PLC 的 I/O 地址按八进制编排：X0-X7, X10-X17, X20-X27 ...

地址池生成函数：

```python
def gen_octal_pool(prefix, count):
    pool = []
    for i in range(count):
        pool.append(f"{prefix}{oct(i)[2:]}")
    return pool
```

- 输入地址池：`X_POOL = gen_octal_pool("X", 16)` → `['X0'..'X7', 'X10'..'X17']`
- 输出地址池：`Y_POOL = gen_octal_pool("Y", 16)` → `['Y0'..'Y7', 'Y10'..'Y17']`
- 池容量 16，足以覆盖所有项目（最大 12 输入 / 8 输出）

### 5.2 随机分配

```python
def random_allocate(points, pool):
    n = len(points)
    addrs = random.sample(pool, n)     # 不重复随机抽取
    return list(zip(points, addrs))
```

- 使用 `random.sample()` 保证项目内地址不重复
- 单选模式下每次只对当前选中项目调用一次

### 5.3 单选模式实现

```python
# 单选变量, 存储当前选中的项目 id
self.selected_project = tk.StringVar(value=PROJECTS[0]["id"])

# Radiobutton 绑定单选变量
rb = ttk.Radiobutton(card, variable=self.selected_project,
                     value=p["id"],
                     command=lambda pid=p["id"]: self._on_select(pid))

# 分配时只处理当前选中项目
def do_allocate(self, silent=False):
    pid = self.selected_project.get()
    p = self._get_project(pid)
    in_alloc = random_allocate(p["inputs"], X_POOL)
    out_alloc = random_allocate(p["outputs"], Y_POOL)
    ...
```

## 六、界面设计

### 6.1 布局

```
+--------------------------------------------------+
|  PLC 实训考核 I/O 随机分配器           [关于]    |
+-------------+------------------------------------+
| 实训项目    |  [当前项目名称]                    |
| (单选)      |  [随机分配] [复制当前] [复制全部]  |
|             |  [导出TXT] [重置]                  |
| (o) 液体混合|  +------+------------+------+----+ |
| ( ) 花样喷泉|  | 序号 | I/O名称   | 类型 |地址| |
| ( ) 交通灯  |  |  1   | 启动      | 输入 | X3 | |
| ( ) 机械手  |  |  2   | 停止      | 输入 | X7 | |
| ( ) 传送线  |  | ...  |           |      |    | |
+-------------+------------------------------------+
| 就绪 · F5 随机分配 · Ctrl+C 复制当前             |
+--------------------------------------------------+
```

### 6.2 配色方案（Windows 11 浅色）

| 元素 | 颜色值 | 说明 |
|------|--------|------|
| 主背景 | #F3F3F3 | Windows 11 浅色背景 |
| 卡片白 | #FFFFFF | 表格/卡片背景 |
| 主题蓝 | #0078D4 | 强调按钮、输入点标签 |
| 输出绿 | #107C10 | 输出点标签 |
| 主文字 | #1A1A1A | 标题/正文 |
| 辅助文字 | #6B6B6B | 状态栏/提示 |

### 6.3 字体

全局统一使用 **Microsoft YaHei UI**（微软雅黑）。

## 七、使用指南

### 7.1 操作流程

1. 启动程序，自动对默认选中项目完成一次随机分配
2. 左侧通过单选按钮选择需要分配的实训项目（每次只能选一个）
3. 点击「随机分配」按钮（或按 F5）对当前选中项目执行随机分配
4. 切换到其他项目后可再次执行随机分配，已分配的结果会保留
5. 点击「复制当前」/「复制全部」复制结果到剪贴板
6. 点击「导出TXT」保存分配结果到文件

### 7.2 快捷键

| 快捷键 | 功能 |
|--------|------|
| F5 | 随机分配（当前选中项目） |
| Ctrl+C | 复制当前项目结果 |

## 八、打包部署

### 8.1 打包方式

采用 PyInstaller `--onedir` 模式，在英文临时目录打包后复制到目标路径，避免中文路径编码问题。

### 8.2 打包参数说明

| 参数 | 说明 |
|------|------|
| --noconsole | 无控制台窗口（GUI 程序） |
| --onedir | 打包为目录形式（含 _internal 依赖目录） |
| --name | 输出文件名（英文） |

### 8.3 运行环境

- 打包后生成 `PLC_IO_RandomAllocator` 目录（含 EXE 和 _internal），无需 Python 环境即可运行
- 支持 Windows 10/11（64 位）

## 九、文件清单

| 文件名 | 说明 |
|--------|--------|
| plc_io_random_allocator.py | 主程序源代码 |
| PLC_IO_RandomAllocator.spec | PyInstaller 打包配置 |
| PLC_IO_RandomAllocator.exe | 打包后的可执行文件 |
| dev_doc.md | 开发文档（Markdown） |
| dev_doc.html | 开发文档（网页） |

## 十、规范符合性说明

| 规范条款 | 符合情况 |
|---------|---------|
| 代码语言优先 Python | 符合，使用 Python 3.14 |
| 开发文档输出 md + html | 符合，同步输出两种格式 |
| 文档字体微软雅黑 | 符合，html 全局设置 Microsoft YaHei |
| 文件存放 E:\TraeWork | 符合，存放于专属文件夹 |
| 专属中文名文件夹 | 符合，文件夹名「PLC考试IO随机分配」 |
| 文件名英文命名 | 符合，所有文件名均为英文 |
| 交付前校验 | 符合，已完成语法验证 + GUI 冒烟测试 + EXE 运行验证 |